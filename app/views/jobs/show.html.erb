<h1> <%= @job.title %> </h1>

<h3> 职位描述 </h3>
<p>
  <%= simple_format(@job.description) %>

  <div class="text-center">
    <%= link_to("投交履历", new_job_resume_path(@job), :style => "font-size: 25px; text-decoration: underline;") %>
  </div>

</p>
